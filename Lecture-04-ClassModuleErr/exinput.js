const rl = require('readline').createInterface({
    input: process.stdin,
    output:process.stdout
});

rl.question('Enter your for name, weight, and height')
    rl.question('Enter your weight (in kg)